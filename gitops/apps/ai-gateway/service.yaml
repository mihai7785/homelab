apiVersion: v1
kind: Service
metadata:
  name: ai-gateway
  namespace: ai-gateway
  labels:
    app.kubernetes.io/name: ai-gateway
spec:
  selector:
    app.kubernetes.io/name: ai-gateway
  ports:
    - port: 80
      targetPort: 8080
      name: http
