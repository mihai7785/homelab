serviceMonitor:
  enabled: true
  additionalLabels:
    release: kube-prometheus-stack

grafanaDashboard:
  enabled: false

controllerManager:
  manager:
    resources:
      limits:
        cpu: 500m
        memory: 128Mi
      requests:
        cpu: 5m
        memory: 64Mi
